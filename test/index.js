const assert = require('assert')
const pando = require('../src/index.js')

describe('Pando', function() {
  
  describe('init', function() {
    
    it('should return -1 when the value is not present', function() {
      
      
      
      assert.equal([1,2,3].indexOf(4), -1);
      
    })
    
  })
  
});